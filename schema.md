# Wiki Schema (LLM-Wiki)

이 문서는 `wiki/`의 구조와 운영 규칙을 정의한다. 본 위키는 **Andrej Karpathy의 LLM-Wiki** 모델을 따른다 — LLM이 직접 위키를 작성·관리하여 지식을 영속적으로 축적하고, 매 질문마다 원문을 재추출하지 않는다.

> 참고: https://news.hada.io/topic?id=28208

## 3계층 아키텍처

| 계층 | 역할 | 이 위키에서의 위치 |
|------|------|-------------------|
| **원문 소스(Sources)** | 불변의 기사·논문·데이터. 읽기 전용 외부 자료. | `sources/*.md` (외부 원문의 요약 노트) |
| **위키(Wiki)** | LLM이 생성·관리하는 구조화된 지식. 개념·엔티티·주제 페이지. | `concepts/*.md` |
| **스키마(Schema)** | LLM에게 구조·컨벤션을 알려주는 설정 문서. | 이 파일 `schema.md` |

본 위키는 프로젝트 독립 레이어이므로 호스트 저장소의 구체 내용(코드 경로·스크립트·결과 파일명)은 포함하지 않는다.

## 디렉토리 구조

```
wiki/
├── schema.md         # 이 문서 (스키마 계층)
├── concepts/         # 위키 계층 (flat): LLM이 유지하는 개념·방법론 페이지
├── sources/          # 원문 소스 계층 (topic-grouped)
│   ├── <topic>/      # 예: ner/, labeling/, env/, dev/
│   └── ...
├── index.md          # (선택) 전체 페이지 카탈로그
└── log.md            # (선택) 인제스트·린트 로그
```

`index.md`는 규모가 작더라도 Ingest마다 갱신하는 것을 기본으로 삼는다 (Karpathy 모델에서 카탈로그는 선택이 아니라 운영의 중심이다). `log.md`는 초기에는 생략 가능하나, 도입 시 §log.md prefix 규칙을 따른다.

### 폴더 정책: sources 는 topic-grouped, concepts 는 flat

소스와 개념은 성격이 다르므로 계층 정책도 다르다.

| 축 | `sources/` | `concepts/` |
|---|---|---|
| 성장 속도 | 읽은 원문 수에 비례 (빠름) | 합성되는 개념 수 (느림) |
| 파일 성격 | 1 파일 = 1 원문 = 단일 도메인 | 여러 출처를 엮어 도메인 교차 |
| 참조 방향 | 주로 수신 (concepts → sources) | 허브 (수·발신 모두) |
| 이동 비용 | 낮음 | 높음 (cross-ref 대규모 갱신) |

**`sources/<topic>/<slug>.md`** — topic 폴더로 묶는다. 현재 운영 중인 topic: `ner/`·`labeling/`·`env/`·`dev/`. 새 topic 은 최소 2개 이상의 원문이 모이고 기존 topic 에 어울리지 않을 때 신설.

**`concepts/<slug>.md`** — 평면 유지. 개념은 주제를 가로지르므로 단일 parent 폴더 선택이 자주 인위적이 되고, concept-to-concept cross-ref 가 허브 구조라 이동 비용이 크다. 재검토 임계치: 페이지 수 > 15, 명확히 구분되는 3개 이상 클러스터 각각 ≥ 4 페이지, 동일 폴더 내 cross-ref 가 폴더간 cross-ref 를 압도.

## 페이지 유형

| 유형 | 위치 | 설명 |
|------|------|------|
| **개념** | `concepts/` | LLM이 요약·통합·갱신하는 구조화 지식 (이론, 방법론, 전략) |
| **소스** | `sources/` | 원문 1건당 1파일. 핵심 요지 + 출처 메타데이터 |

## 수록 원칙

1. **프로젝트 독립성**: 특정 저장소의 파일 경로·스크립트·결과 파일명을 포함하지 않는다.
2. **개발·기술 내용**: 재사용 가능한 지식(이론·알고리즘·방법론·원문 요약)만 담는다.
3. **LLM 관리 대상**: `concepts/`는 LLM이 인제스트/린트 과정에서 계속 갱신한다. 사람 편집도 가능하나, 구조 일관성은 스키마 규칙을 따른다.

## 3대 작업 (Operations)

LLM-Wiki의 세 가지 핵심 작업이다. 각각 트리거와 절차가 정해져 있다.

### 1. 인제스트 (Ingest) — 새 소스 추가

**트리거**: 새 논문·기사·블로그·데이터를 위키에 반영할 때.

**절차**:
1. 원문을 읽고 핵심을 추출
2. `sources/<slug>.md`에 원문 요약 노트 작성 (템플릿 §페이지 템플릿 참조)
3. 기존 `concepts/` 페이지 중 이 소스가 갱신해야 할 페이지를 식별 — 10~15개까지도 연쇄적으로 업데이트될 수 있음
4. 새 개념이 등장하면 `concepts/<slug>.md`를 신규 생성
5. 관련 페이지 간 교차 참조 추가 (§교차 참조 규칙)
6. `log.md`가 있다면 한 줄 기록

### 2. 쿼리 (Query) — 지식 조회

**트리거**: 위키 기반 질문·답변이 필요할 때.

**절차**:
1. 질문과 관련된 `concepts/` 페이지를 찾는다 (`index.md`가 있으면 우선 활용)
2. 해당 개념의 주장을 근거로 답변을 합성한다
3. 답변에 인용한 `concepts/` 및 `sources/` 페이지 경로를 명시한다
4. 위키에 정보가 없거나 낡아 보이면 인제스트 트리거로 연결
5. **환원(복리 효과)**: 답변이 재사용 가치가 있다고 판단되면 휘발시키지 않고 `concepts/`에 새 페이지로 파일링하거나 기존 페이지에 병합한다. 이것이 Karpathy 모델의 복리 메커니즘이다 — 좋은 질문·답변이 곧 위키의 성장이다.

### 3. 린트 (Lint) — 품질 유지

**트리거**: 주기적(예: 월 1회) 또는 대규모 인제스트 직후.

**점검 항목**:
- 모순된 진술 (같은 사실에 대한 불일치)
- 낡은 주장 (새 소스로 대체되어야 하는 내용)
- 고아 페이지 (어디서도 참조되지 않는 페이지)
- 누락된 참조 (개념 페이지가 `sources/`를 인용하지 않음)
- 끊어진 링크
- 수록 원칙 위반 (프로젝트 경로·스크립트·결과 파일명 등장)

**결과**: 수정 커밋과 `log.md` 기록.

## 교차 참조 규칙

- 위키 내 참조는 `wiki/` 기준 상대 경로
  - 예: `` `concepts/bio-tagging.md` ``, `` `sources/ner/park-2021-klue.md` ``
- `concepts/` 페이지는 자신의 주장을 뒷받침하는 `sources/` 파일을 반드시 인용한다
- `sources/` 페이지는 자신을 인용하는 `concepts/` 페이지를 역링크로 기록한다
- 외부(호스트 저장소) 참조는 위키에 직접 기재하지 않는다 — 호스트 저장소 쪽에서 링크한다

## 페이지 템플릿

### `concepts/*.md`

```markdown
# <개념명>

> 한 줄 요약

## 개요
## 핵심 원칙
## 세부 설명
## 출처
- `sources/<file>.md` — <이 출처에서 가져온 핵심>
```

### `sources/*.md`

```markdown
# <저자> (<연도>) — <제목>

- **저자**:
- **연도**:
- **매체/학회**:
- **링크**:
- **유형**: 1차 문헌 | 2차 자료

## 핵심 요지

## 주요 내용

## 인용하는 위키 페이지
- `concepts/<file>.md`
```

### (선택) `index.md`

전체 페이지 카탈로그. 임베딩 없이도 LLM이 관련 페이지를 빠르게 찾도록 돕는다.

```markdown
# Index

## Concepts
- `concepts/bio-tagging.md` — BIO/IOB 태그 체계와 span 변환
- `concepts/token-management.md` — LLM 컨텍스트 운영 전략
- ...

## Sources
- `sources/ner/ramshaw-marcus-1995-bio-tagging.md` — BIO 스킴 원조 논문
- ...
```

### (선택) `log.md`

인제스트·린트 이력의 시간순 append-only 로그. CLI grep이 가능하도록 **파싱 가능한 prefix 규칙**을 따른다:

```
## [YYYY-MM-DD] <op> | <title>
```

- `<op>` ∈ `ingest` | `lint` | `query-filed` (쿼리 환원)
- 한 줄 요지를 바로 아래 불릿으로 남긴다

예시:

```markdown
# Log

## [2026-04-13] ingest | addyosmani/agent-skills
- concepts/agent-skills.md 생성, sources/addyosmani-agent-skills.md 작성
## [2026-04-13] lint | bio-tagging
- 끊어진 참조 1건 수정
## [2026-04-14] query-filed | BIO vs IOB2 선택 근거
- concepts/bio-tagging.md §결정 근거 절에 병합
```

## 비고

> **유지 비용의 본질**: 지식 베이스 유지의 고통은 읽기·사고가 아니라 bookkeeping(크로스 링크 갱신·참조 일관성·고아 페이지 점검)이다. LLM이 그 부담을 대신 지므로 사람은 방향성·검수에 집중한다. — Karpathy

- "LLM이 프로그래머, Obsidian이 IDE, 위키가 코드베이스" — 유지 비용을 LLM이 감당하므로 사람은 방향성·검수에 집중한다.
- `qmd`(BM25/벡터 하이브리드 로컬 검색) 같은 외부 도구는 이 스키마와 호환되지만 필수는 아니다.
